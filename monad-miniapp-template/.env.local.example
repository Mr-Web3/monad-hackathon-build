# ===========================================
# Monad VWAP Demo — copy to .env.local (or .env)
# NEVER commit .env / .env.local (they are in .gitignore)
# ===========================================

# Reown AppKit (WalletConnect). Get project ID: https://dashboard.reown.com
NEXT_PUBLIC_REOWN_PROJECT_ID=""

# Monad Testnet RPC. Default: https://testnet-rpc.monad.xyz (avoid Alchemy — can 500)
NEXT_PUBLIC_RPC_URL="https://testnet-rpc.monad.xyz"

# Monad Testnet chain ID
NEXT_PUBLIC_CHAIN_ID="10143"

# VWAPDemo contract address (Monad Testnet). Set after deploying from Hardhat.
NEXT_PUBLIC_VWAP_DEMO_ADDRESS=""

# Block explorer base URL (for “View on Explorer” links)
NEXT_PUBLIC_EXPLORER_BASE_URL="https://testnet.monadscan.com"

# Optional: Faucet URL for “Get faucet MON”
# NEXT_PUBLIC_FAUCET_URL="https://faucet.monad.xyz"

# Optional: App URL for Farcaster embed (use ngrok/cloudflared URL for local dev)
# NEXT_PUBLIC_URL=""
